# Required for the exact routed IPv6 VPN endpoint tuple. private-vm keeps
# global IPv4 forwarding disabled and never changes host-global sysctls during
# session allocation or cleanup.
net.ipv6.conf.all.forwarding = 1
